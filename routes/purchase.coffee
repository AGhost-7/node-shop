db = require('../utils/db')
router = require('express').Router()

# View purchase with subtotal and total
router
.get('/', (req, res, next) ->
  db((err, query, done) ->

  )
)

# accept to purchase and proceed to pay using w/e...
